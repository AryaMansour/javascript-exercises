const getTheTitles = (first) => {
    let title = []
    for (i=0; i<first.length; ++i) {
        title.push(first[i].title)
    }
    return title;
  };
// Do not edit below this line
module.exports = getTheTitles;
